Requirements:

Python Libraries: pandas, art

pip install manager
pip install pandas
pip install art

1. Place the .csv file in the same directory as the script.
2. Run the script and copy/paste the full name of .csv file.
3. Once completed, the results are stored in the same directory: output.csv.